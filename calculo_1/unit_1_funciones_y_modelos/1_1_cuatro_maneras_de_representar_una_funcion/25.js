// Si f (x) = 3x^2 -x + 2, encuentre f (2), f (-2)

function f(x) {
  return (3 * (x ** 2)) - x + 2
}

console.log(f(2)) // 12 
console.log(f(-2)) // 16

// las otras preguntas de f(x) involucran algebra, por lo que no se puede programar por ahora.